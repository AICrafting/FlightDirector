---
name: tests
on:
  workflow_dispatch:

  push:
    branches: [develop]
  pull_request: {}

jobs:
  script-tests:
    name: script tests
    runs-on: ubuntu-latest
    container:
      image: alpine:latest
    steps:
      - name: Setup tools
        run: |
          # python3: the prompt-logger producers/summary (stdlib only) and their tests
          apk add --update npm bash gawk git coreutils jq python3

      - name: Checkout
        uses: actions/checkout@v6

      - name: Run script tests
        run: |
          bash scripts/runTests.sh
